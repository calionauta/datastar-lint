version: "2"

run:
  timeout: 3m

formatters:
  enable:
    - gofumpt
    - goimports

linters:
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - bodyclose

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
